<!DOCTYPE html>
<html lang="pt-BR">

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="adm.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Arvo:ital,wght@0,400;0,700;1,400;1,700&display=swap"
        rel="stylesheet">

    <title>Inspeção</title>
</head>
<body>
    <header>
        <img src="../public/img/logo.png" alt="Logo Autofix">
    </header>


        <div class="container">
            <h3 class="title">Civic - Type R </h3>

        <div class="d-flex justify-content-center">
            <div class="inputs" id="inputs">
                <div class="form-group mt-4">
                    <input type="text" class="form-control" placeholder="Digite um novo item...">
                </div>
              
            </div>
        </div>

        <div class="text-center mt-4">
            <button id="addInput" class="btn btn-primary">+</button>
        </div>

        <div class="text-center mt-4 d-flex justify-content-center gap-3">
            <button class="btn btn-success">Enviar</button>
            <a href="/home_adm"><button class="btn btn-danger" id="confirmExit">Cancelar</button></a>
        </div>
    </div>
</div>
    <!-- Modal de confirmação -->
    <div class="modal-overlay" id="modalOverlay">
        <div class="modal-content">
            <h4>Deseja sair sem salvar?</h4>
            <button class="btn-exit" id="confirmExit">Sair mesmo assim</button>
            <button class="btn-cancel" id="cancelExit">Cancelar</button>
        </div>
    </div>

    <script>
        // Função para adicionar novo input
        document.getElementById('addInput').addEventListener('click', function() {
            const newInput = document.createElement('div');
            newInput.classList.add('form-group', 'mt-4');
            newInput.innerHTML = '<input type="text" class="form-control" placeholder="Digite um novo item...">';
            document.getElementById('inputs').appendChild(newInput);
        });

        // Abrir o modal ao clicar na seta de voltar
        document.getElementById('backButton').addEventListener('click', function(event) {
            event.preventDefault(); // Impede a navegação imediata
            document.getElementById('modalOverlay').style.display = 'flex';
        });

        // Fechar o modal e sair da página
        document.getElementById('confirmExit').addEventListener('click', function() {
            window.location.href = 'inspecao_manutencao_pendente.html'; // Redireciona para a página de manutenção pendente
        });

        // Fechar o modal e cancelar a ação
        document.getElementById('cancelExit').addEventListener('click', function() {
            document.getElementById('modalOverlay').style.display = 'none';
        });
    </script>
</body>
<style>
    body {
        font-family: 'Arvo', serif;
        margin: 0;
        padding: 0;
        background-color: #f4f4f4;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        font-weight: normal;
    }

    header {
        background-color: #000000;
        color: white;
        padding: 8px;
        text-align: center;
        position: relative;
    }

    .title {
        text-align: center;
        font-size: 26px;
        margin: 10px auto;
        color: #333;
        width: 100%;
        
        justify-content: center; /* Centraliza o título */
        position: relative; /* Adiciona posição relativa para o título */
    }

    .whatsapp-icon {
        position: absolute;
        right: 30px; /* Ajuste para mover o ícone mais para a direita */
        top: 50%;
        transform: translateY(-50%); /* Centraliza verticalmente */
        width: 46px; /* Ajuste o tamanho do ícone */
        height: 46px; /* Ajuste o tamanho do ícone */
        cursor: pointer;
        border: none;
        background: none;
        padding: 0;
    }

    .container {
        max-width: 900px;
        padding: 30px;
        margin: 100px auto;
        background-color: #fff;
        border-radius: 5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        text-align: center;
    }

    header img {
        width: 200px;
        height: auto;
    }

    .welcome-message {
        position: absolute;
        top: 30px;
        right: 10px;
        font-size: 16px;
        color: white;
        font-weight: bold;
    }

    .text-center {
        margin-top: 20px;
    }

    .inputs {
        border-radius: 5px;
        width: 100%;
        max-width: 500px;
        align-items: center;
    }

    .btn-success {
    margin-top: 20px;
    width: 100px;
    height: 40px; /* Ajuste a altura do botão */
    background-color: #138132;
    color: aliceblue;
    border-radius: 5px;
    border: none;
    align-items: center;
    justify-content: center;
    text-align: center;
    cursor: pointer; /* Adiciona um cursor para indicar que é clicável */
}

.btn-danger {
    margin-top: 20px;
    width: 100px;
    height: 40px; /* Ajuste a altura do botão */
    background-color: #cc0606;
    color: aliceblue;
    border-radius: 5px;
    border: none;
    align-items: center;
    justify-content: center;
    text-align: center;
    cursor: pointer; /* Adiciona um cursor para indicar que é clicável */
}


    .title-container {
        display: flex;
        align-items: center;
        justify-content: center;
    }

    /* Estilos para o modal */
    .modal-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        justify-content: center;
        align-items: center;
    }

    .modal-content {
        background-color: white;
        padding: 20px;
        border-radius: 5px;
        text-align: center;
        width: 300px;
    }

    .modal-content h4 {
        margin-bottom: 20px;
    }

    .modal-content button {
        margin: 5px;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }

    .btn-cancel {
        background-color: #f0f0f0;
        color: #000;
    }

    .btn-exit {
        background-color: #d9534f;
        color: #fff;
    }
</style>
</html>